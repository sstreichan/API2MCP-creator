// Package mcp adapts the MCP SDK: server instance, tool registration, and
// handlers.
//
// The SDK is pinned here as a blank import until the adapter is implemented,
// so that go.mod/go.sum record the v1.8.0 dependency from the scaffold phase on.
package mcp

import _ "github.com/modelcontextprotocol/go-sdk/mcp"
